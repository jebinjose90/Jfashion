<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Inria+Sans%3A400%2C700" />
    <style>
        /* Custom Styles */
        .input-container input::placeholder {
            color: lightgray;
            font-family: 'Inria Sans', sans-serif;
            /* Apply the Google Font to the placeholder */
        }

        .text {
            color: rgb(112, 112, 112);
            /* Set the default color */
            font-family: 'Inria Sans', sans-serif;
            /* Apply the Google Font to the text */
        }

        .text-highlight {
            color: #6badeb;
            /* Set the highlighted color */
            font-family: 'Inria Sans', sans-serif;
            /* Apply the Google Font to the highlighted text */
        }

        .signup-link {
            text-decoration: underline;
            color: #6badeb;
            /* Set the color for the underlined link */
            font-family: 'Inria Sans', sans-serif;
            /* Apply the Google Font to the link */
        }

        .gethelp-link {
            text-decoration: underline;
            color: #6badeb;
            /* Set the color for the underlined link */
            font-family: 'Inria Sans', sans-serif;
            /* Apply the Google Font to the link */
        }

        .timer {
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: start;
            font-family: 'Inria Sans', sans-serif;
            color: rgb(98, 98, 98);
            margin: 10px 0;
        }

        .timer button {
            background: none;
            outline: none;
            border: none;
            color: #6badeb;
            font-family: 'Inria Sans', sans-serif;
            text-decoration: underline;
            cursor: pointer;
        }

        .timer button:disabled {
            color: rgb(200, 200, 200);
            cursor: auto;
        }

        .timer .time {
            width: 30px;
        }
    </style>
    </style>
</head>

<body style="background-color: rgba(107, 173, 235, 0.5);">


    <!-- Bootstrap Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand "style="margin-left: 10px;">
            <img src="/img/company_logo.png" alt="Your Logo" height="50">
        </a>
    </nav>